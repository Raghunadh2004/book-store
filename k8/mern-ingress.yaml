apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mern-ingress
  namespace: mern-app
  labels:
    name: fintrack-ingress
spec:
  rules:
    - host: books
      http:
        paths:
          - path: "/frontend"
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 5173
          - path: "/backend"
            pathType: Prefix
            backend:
              service:
                name: backend-service
                port:
                  number: 5000